export declare class JsonPropertyFilter {
    private static DEFAULT_INCLUDE_SYMBOL;
    private static EXCLUDE_SYMBOL;
    private static FILTER_SEPARATOR;
    private static INCLUDE_SYMBOL;
    private _excludeFilters;
    private _includeFilters;
    constructor(filters?: string | string[], separator?: string, excludeSymbols?: RegExp[], includeSymbols?: RegExp[]);
    readonly excludeFilters: string[];
    readonly includeFilters: string[];
    apply(source: object | object[]): {};
    setExcludeFilters(filters: string | string[], separator?: string, symbols?: RegExp[]): void;
    setIncludeFilters(filters: string | string[], separator?: string, symbols?: RegExp[]): void;
    private _applyInclude(source);
    private _applyExcludeFilters(source);
    private _assertFilters(filters);
    private _assertSeparator(separator?);
    private _formatProperties(properties, separator?);
}
