export declare class JsonExcludePropertyFilter {
    private readonly _properties;
    private static readonly ALL_PROPERTIES_REGEX;
    private static readonly ALL_ELEMENT_PROPERTIES_REGEX;
    private static readonly ARRAY_INDEX;
    private static readonly ARRAY_INDEX_START;
    private static readonly PATH_SEPARATOR;
    private static readonly STRING_EMPTY;
    constructor(_properties: string[]);
    apply(source: string[]): string[];
    private _exclude(rule, source);
    private _excludeProperty(rule, source, path);
    private _excludeProperties(rule, source);
    private _excludeRootProperty(rule, path, source);
    private _excludeRootProperties(rule, source);
    private _excludeSpecificPath(rule, source);
    private _removeArrayIndex(path);
    private _removeArrayIndexStart(path);
}
